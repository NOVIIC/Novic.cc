---
title: 'MDX 排版能力演示'
description: '标题、列表、引用、代码、表格、脚注、任务列表、组件——一篇用来验证 MDX 全部语法的样板。'
pubDate: 2026-06-30
updatedDate: 2026-07-01
tags: ['mdx', '测试']
---

这是一篇用于验证 MDX 排版能力的样板文章，覆盖几乎所有常见（与不常见）的语法元素，方便日后排查渲染问题。

## 文本样式

支持 **加粗**、*斜体*、***加粗斜体***、~~删除线~~、`行内代码`，以及 [链接](https://astro.build)。

还可以使用上下标（借助 HTML）：H<sub>2</sub>O、x<sup>2</sup>。

## 标题层级

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

## 列表

### 无序列表

- 第一项
  - 嵌套项 A
    - 更深嵌套
  - 嵌套项 B
- 第二项
- 第三项

### 有序列表

1. 步骤一
2. 步骤二
   1. 子步骤 2.1
   2. 子步骤 2.2
3. 步骤三

### 任务列表

- [x] 已完成的事项
- [x] 搭建博客框架
- [ ] 未完成的事项
- [ ] 写更多文章

## 引用

> 简单胜于复杂。
> —— 某位工程师

> 多行引用的第二行。
>
> 中间空行分隔的第三段。

## 代码块

### 带行号与语言标签

```py showLineNumbers
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("Novic"))
```

### 行高亮

```js {2,4} showLineNumbers
const features = ['highlight', 'line-numbers', 'copy'];
features.forEach((f) => console.log(f));
// 上面第 2、4 行会被高亮
features.reverse();
```

### 带标题的代码块

```ts title="example.ts"
const answer: number = 42;
console.log(`The answer is ${answer}`);
```

### 纯文本代码块

```
没有语言标签的代码块
保留   空白
```

## 表格

| 工具           | 作用         | 是否启用 |
| -------------- | ------------ | -------- |
| Astro          | 框架         | ✅       |
| Tailwind       | 样式         | ✅       |
| Pagefind       | 搜索         | ✅       |
| ExpressiveCode | 代码块美化   | ✅       |
| MDX            | 组件增强     | ✅       |

### 对齐表格

| 左对齐         | 居中对齐       | 右对齐         |
| :------------- | :------------: | -------------: |
| Left           | Center         | Right          |
| 靠左           | 居中           | 靠右           |

## 分隔线

---

上方与下方各一条分隔线。

## 图片

![Astro Logo](https://astro.build/assets/brand/web/favicon.svg)

## 脚注

这里有一个脚注引用[^1]，还有一个长脚注[^longnote]。

[^1]: 这是第一个脚注的内容。
[^longnote]: 这是一个包含多段内容的脚注。

    后续段落需要缩进以包含在脚注里。

## 定义列表（HTML）

<dl>
  <dt>Astro</dt>
  <dd>现代静态站点生成框架。</dd>
  <dt>MDX</dt>
  <dd>Markdown + JSX 的混合体。</dd>
</dl>

## 键盘按键

可以用 <kbd>Ctrl</kbd> + <kbd>K</kbd> 打开搜索，<kbd>Esc</kbd> 关闭。

## 数学符号（纯文本）

行内：当 n → ∞ 时，(1 + 1/n)^n → e。

块级（借助 HTML 排版）：

<div class="text-center">

∫₀^∞ e^(-x²) dx = √π / 2

</div>

## 嵌入式 HTML

<div class="not-prose my-4 rounded border-l-4 border-[var(--accent)] bg-[var(--accent)]/10 p-4">
  这是一段直接用 HTML 写的<strong>高亮提示框</strong>，MDX 允许在 Markdown 中混写 HTML。
</div>

## 链接引用

这是一个[引用式链接][ref-link]，还有[另一个][ref-two]。

[ref-link]: https://astro.build "Astro 官网"
[ref-two]: https://tailwindcss.com "Tailwind CSS"

## 转义字符

星号 \*、反斜杠 \\、反引号 \`、花括号 \{ \} 都可以被转义。

## 自动链接

普通 Markdown 自动链接：[https://astro.build](https://astro.build)、[novic@example.com](mailto:novic@example.com)。

---

正文结束。如果你看到这段文字且以上所有元素都正确渲染，说明排版系统工作正常。
